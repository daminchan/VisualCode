# Visual Code Notebook - コンセプト

## アプリ概要

**「動くコード」をビジュアル付きで保存できるスクラップブック**

コードを貼り付けるだけで、リアルタイムプレビュー＆保存。
いつでも見返せる、自分だけのビジュアルライブラリを構築する。

---

## 従来の問題

```
┌────────────────────────────────────────────────┐
│  従来のノート                                   │
│  📝 テキストでメモ → 文字だらけで見返しにくい   │
│  📸 スクショで保存 → 編集できない、検索しにくい │
│  🔖 ブックマーク → リンク切れ、自分のものにならない │
└────────────────────────────────────────────────┘
```

## 解決策

```
┌────────────────────────────────────────────────┐
│  Visual Code Notebook                          │
│  🎨 コードで保存 → 実際に動く状態で蓄積        │
│  ✏️ いつでも編集可能                           │
│  👀 ビジュアルで直感的に見返せる               │
└────────────────────────────────────────────────┘
```

---

## 使用例

### 1. 学習ノートとして

**「テキストノート」ではなく「自分専用のProgate」を作る**

```
① Geminiに質問
   「フェッチの仕組みを図解して」
        ↓
② Geminiがコード生成
   Progateみたいな見やすいReactコンポーネント
        ↓
③ アプリに貼り付け
        ↓
④ プレビューで確認
        ↓
⑤ 保存
        ↓
⑥ いつでも見返せる
   自分だけの「ビジュアル学習ライブラリ」完成
```

- 学んだことを**見やすい解説ページ**として蓄積
- 後から見返すと**すぐ思い出せる**
- 自分の理解レベルに合わせた**オリジナル教材集**

---

### 2. UIアイデア帳として

ひらめいたUIデザインをコードで保存

```
① 「こんなUIいいな」とひらめく
        ↓
② Gemini/自分でコード化
        ↓
③ アプリに保存
        ↓
④ 後日、実装時に参考にする
```

- デザインのインスピレーションを蓄積
- 実際に動く状態で保存されるので、挙動も確認できる

---

### 3. コンポーネントライブラリとして

自作のUI部品を整理・管理

```
① ボタン、カード、フォームなどを作成
        ↓
② アプリに保存
        ↓
③ プロジェクトごとに再利用
```

- 自分だけのUIキット
- コピペですぐ使える

---

## コア機能

| 機能 | 説明 |
|------|------|
| コード貼り付け | Reactコードをそのまま貼り付け |
| リアルタイムプレビュー | 貼り付けた瞬間に描画 |
| 保存 | タイトル付きで永続保存 |
| 一覧表示 | 保存したコンテンツをリスト表示 |
| 編集 | 保存後も自由に編集可能 |
| 削除 | 不要なものは削除 |

---

## ターゲットユーザー

- プログラミング学習者
- フロントエンド開発者
- UIデザインに興味がある人
- 視覚的に学習したい人

---

## 技術スタック

### 全体アーキテクチャ図

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Turborepo (モノレポ管理)                          │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │  pnpm workspace                                                     ││
│  │                                                                     ││
│  │  ┌─────────────────────┐      ┌─────────────────────┐              ││
│  │  │  apps/web           │      │  apps/api           │              ││
│  │  │  ─────────────────  │      │  ─────────────────  │              ││
│  │  │  Next.js 15         │ ←──→ │  Hono               │              ││
│  │  │  shadcn/ui          │ RPC  │  Hono/OpenAPI       │              ││
│  │  │  React Aria         │      │         │           │              ││
│  │  │  Tailwind v4        │      │         ↓           │              ││
│  │  └─────────────────────┘      │  Drizzle ORM        │              ││
│  │                               └─────────┼───────────┘              ││
│  │  ┌─────────────────────┐                ↓                          ││
│  │  │  packages/ui        │      ┌─────────────────────┐              ││
│  │  │  ─────────────────  │      │  Turso (SQLite)     │              ││
│  │  │  共有UIコンポーネント │      │  エッジDB            │              ││
│  │  └─────────────────────┘      └─────────────────────┘              ││
│  │                                                                     ││
│  │  ┌─────────────────────┐      ┌─────────────────────┐              ││
│  │  │  packages/db        │      │  NextAuth (Auth.js) │              ││
│  │  │  ─────────────────  │      │  ─────────────────  │              ││
│  │  │  Drizzle スキーマ    │      │  認証・セッション管理 │              ││
│  │  └─────────────────────┘      └─────────────────────┘              ││
│  └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 各技術の解説

#### 1. Turborepo - モノレポ管理ツール

**「モノレポ」とは？**
```
従来（マルチレポ）:
  repo-frontend/    ← 別々のリポジトリ
  repo-backend/     ← 管理が大変
  repo-shared/      ← バージョン合わせが面倒

モノレポ:
  my-project/
    ├── apps/web/      ← 1つのリポジトリに
    ├── apps/api/      ← 全部まとめる
    └── packages/ui/   ← 共有コードも簡単
```

**なぜTurborepoを使うのか？**
- ビルドの**キャッシュ**が賢い（変更のないパッケージはスキップ）
- 複数パッケージを**並列ビルド**できる
- フロントとバックエンドで**型を共有**できる

**ベストプラクティス（2025）**
- `apps/` にアプリケーション、`packages/` にライブラリを配置
- `@repo/` ネームスペースでパッケージ名を統一（npm競合回避）
- lockfileを必ずコミット（再現性確保）

> 参考: [Structuring a repository | Turborepo](https://turborepo.com/docs/crafting-your-repository/structuring-a-repository)

---

#### 2. pnpm - パッケージマネージャー

**npm / yarn との違い**
```
npm/yarn:
  node_modules/
    └── lodash/  ← プロジェクトごとにコピー（容量大）

pnpm:
  .pnpm-store/
    └── lodash/  ← 1箇所に保存（ハードリンクで共有）
```

**なぜpnpmを使うのか？**
- **高速**：既にダウンロード済みのパッケージは再利用
- **省スペース**：ディスク容量を節約
- **厳格**：依存関係の問題を早期発見（ゴースト依存防止）

> 参考: [How we configured pnpm and Turborepo | Nhost](https://nhost.io/blog/how-we-configured-pnpm-and-turborepo-for-our-monorepo)

---

#### 3. Next.js 15 - フロントエンドフレームワーク

**Reactとの違い**
```
React単体:
  - ルーティングは自分で設定
  - SSRは自分で実装
  - 最適化は自分で考える

Next.js:
  - ルーティング自動（ファイルベース）
  - SSR/SSG組み込み
  - 画像最適化、フォント最適化など標準装備
```

**App Router のフォルダ規約**
```
app/
├── page.tsx           # / ルート
├── layout.tsx         # 共通レイアウト
├── loading.tsx        # ローディングUI
├── error.tsx          # エラーバウンダリ
├── (auth)/            # Route Group（URLに影響しない）
│   ├── login/
│   └── signup/
├── dashboard/
│   └── [id]/          # 動的ルート /dashboard/123
└── _components/       # プライベートフォルダ（ルーティング対象外）
```

**ベストプラクティス（2025）**
- `src/` ディレクトリを使用（設定とソースを分離）
- Route Grouping `(auth)`, `(dashboard)` で論理的に整理
- `_components/` でルート専用コンポーネントを管理
- 200+ファイルをcomponentsに詰め込まない → サブディレクトリに分割

> 参考: [Getting Started: Project Structure | Next.js](https://nextjs.org/docs/app/getting-started/project-structure)

---

#### 4. Hono - 軽量Webフレームワーク

**Expressとの違い**
```
Express:
  - Node.js専用
  - 歴史が長い、情報多い
  - やや重い

Hono:
  - エッジランタイム対応（Cloudflare Workers等）
  - TypeScript最優先
  - 超軽量・超高速（~14KB）
```

**なぜHonoを使うのか？**
- **型安全**：TypeScriptと相性抜群
- **軽量**：バンドルサイズが小さい
- **RPC機能**：フロントから型安全にAPI呼び出し

> 参考: [Best Practices - Hono](https://hono.dev/docs/guides/best-practices)

---

#### 5. Hono RPC - 型安全なAPI呼び出し

**従来のAPI呼び出し**
```typescript
// フロント側：型がない、タイポしても気づかない
const res = await fetch('/api/users')
const data = await res.json() // any型...
```

**Hono RPCを使うと**
```typescript
// フロント側：型が自動補完される！
const res = await client.api.users.$get()
const data = await res.json() // { id: string, name: string }[] 型付き！
```

**ベストプラクティス（2025）**
- モノレポで `strict: true` を設定（型推論に必要）
- ルートが多い場合は分割（IDE性能のため）
- フロントとバックエンドでHonoバージョンを揃える

> 参考: [RPC - Hono](https://hono.dev/docs/guides/rpc)

---

#### 6. Turso - エッジデータベース

**従来のDBとの違い**
```
従来のDB (PostgreSQL等):
  - 1箇所のサーバーにデータ
  - 遠い地域からは遅延が発生

Turso (LibSQL):
  - SQLiteベース
  - 世界中のエッジにレプリカ配置
  - ユーザーの近くから高速応答
```

**なぜTursoを使うのか？**
- **高速**：エッジで動くので低レイテンシ
- **安価**：無料枠が大きい（9GB、500DBまで無料）
- **SQLite互換**：学習コスト低い

> 参考: [Drizzle + Turso - Turso](https://docs.turso.tech/sdk/ts/orm/drizzle)

---

#### 7. Drizzle ORM - 型安全なORM

**従来のORM（Prisma）との違い**
```
Prisma:
  - スキーマファイルが独自形式（.prisma）
  - 生成されるクライアントが重い
  - エッジランタイムで動かしにくい

Drizzle:
  - TypeScriptでスキーマ定義
  - 軽量（~7.4KB）、エッジ対応
  - SQLに近い書き方
```

**ベストプラクティス（2025）**
```
packages/db/
├── src/
│   ├── index.ts      # DB接続エクスポート
│   ├── schema.ts     # テーブル定義
│   └── queries/      # クエリ関数
├── migrations/       # マイグレーションファイル
└── drizzle.config.ts # Drizzle設定
```

> 参考: [Drizzle ORM - Drizzle with Turso](https://orm.drizzle.team/docs/tutorials/drizzle-with-turso)

---

#### 8. shadcn/ui - UIコンポーネント集

**従来のUIライブラリとの違い**
```
MUI, Chakra UI:
  - npmでインストール
  - カスタマイズしにくい
  - バンドルサイズ大

shadcn/ui:
  - コードをコピーしてプロジェクトに配置
  - 自由にカスタマイズ可能
  - 必要なものだけ使う
```

**2025年の変更点**
- Tailwind v4 完全対応
- HSL → OKLCH カラー移行
- `data-slot` 属性でスタイリング
- `forwardRef` 廃止 → `ComponentProps` へ

> 参考: [Tailwind v4 - shadcn/ui](https://ui.shadcn.com/docs/tailwind-v4)

---

#### 9. Tailwind CSS v4 - スタイリング

**v4の大きな変更**
```
v3以前:
  tailwind.config.js で設定

v4:
  CSS内で @theme ディレクティブで設定
  → tailwind.config.js 不要！
```

**CSS-first configuration**
```css
@import "tailwindcss";

@theme {
  --color-primary: oklch(0.7 0.15 200);
  --font-display: "Inter", sans-serif;
}
```

**ベストプラクティス（2025）**
- `@theme inline` でカスタムプロパティ定義
- `tailwindcss-animate` → `tw-animate-css` に移行
- `size-*` ユーティリティ活用（`w-* h-*` の代わり）

> 参考: [Building Scalable UI Systems with Tailwind CSS v4](https://dev.to/shoaibsid/building-scalable-ui-systems-with-tailwind-css-v4-and-shadcnui-11g0)

---

#### 10. React Aria Components - アクセシブルUI

**なぜ必要？**
```
自作ボタン:
  <div onClick={...}>クリック</div>
  → キーボード操作不可、スクリーンリーダー対応なし

React Aria:
  <Button onPress={...}>クリック</Button>
  → キーボード、スクリーンリーダー、全対応
```

**なぜReact Ariaを使うのか？**
- **アクセシビリティ**：WCAG準拠が自動
- **ヘッドレス**：スタイルは自由（Tailwindと相性◎）
- **Adobe製**：品質が高い

---

#### 11. NextAuth (Auth.js v5) - 認証

**なぜNextAuthを使うのか？**
```
Supabase Auth:
  - Supabase DBとセットで使うと強力
  - このプロジェクトはTursoを使うため、メリット減

NextAuth (Auth.js):
  - Next.js専用設計、App Router完全対応
  - Drizzle公式アダプターあり
  - ベンダーロックインなし
  - 完全なデータ所有権
```

**特徴**
- **Next.js統合**：App Router、Server Componentsと完全連携
- **Drizzleアダプター**：`@auth/drizzle-adapter` 公式サポート
- **プロバイダー豊富**：Google, GitHub, Discord等
- **無料**：制限なし

**セットアップ構成**
```
apps/web/
├── src/
│   ├── auth.ts           # NextAuth設定
│   ├── auth.config.ts    # 認証設定（adapter不要な部分）
│   └── app/
│       └── api/
│           └── auth/
│               └── [...nextauth]/
│                   └── route.ts  # 認証APIルート
```

> 参考: [Auth.js | Drizzle Adapter](https://authjs.dev/getting-started/adapters/drizzle)

---

### プロジェクト構成（ベストプラクティス準拠）

```
visual-code-notebook/
│
├── apps/
│   ├── web/                      # Next.js 15 フロントエンド
│   │   ├── src/
│   │   │   ├── app/
│   │   │   │   ├── (auth)/       # 認証系Route Group
│   │   │   │   │   ├── login/
│   │   │   │   │   │   └── page.tsx
│   │   │   │   │   └── signup/
│   │   │   │   │       └── page.tsx
│   │   │   │   ├── (main)/       # メイン機能Route Group
│   │   │   │   │   ├── dashboard/
│   │   │   │   │   │   └── page.tsx
│   │   │   │   │   └── notebook/
│   │   │   │   │       └── [id]/
│   │   │   │   │           └── page.tsx
│   │   │   │   ├── layout.tsx
│   │   │   │   ├── page.tsx      # ランディングページ
│   │   │   │   └── globals.css   # Tailwind v4 @theme
│   │   │   ├── components/
│   │   │   │   ├── ui/           # shadcn/ui コンポーネント
│   │   │   │   ├── layout/       # Header, Footer, Sidebar
│   │   │   │   └── features/     # 機能別コンポーネント
│   │   │   │       ├── editor/   # コードエディタ関連
│   │   │   │       └── preview/  # プレビュー関連
│   │   │   ├── lib/
│   │   │   │   └── api.ts        # Hono RPC クライアント
│   │   │   ├── auth.ts           # NextAuth設定
│   │   │   ├── auth.config.ts    # 認証設定
│   │   │   └── hooks/            # カスタムフック
│   │   ├── public/
│   │   ├── next.config.ts
│   │   ├── tailwind.config.ts    # v4では最小限 or 不要
│   │   └── package.json
│   │
│   └── api/                      # Hono バックエンド
│       ├── src/
│       │   ├── index.ts          # エントリーポイント
│       │   ├── routes/
│       │   │   ├── index.ts      # ルート集約
│       │   │   ├── notebooks.ts  # ノートCRUD
│       │   │   └── auth.ts       # 認証エンドポイント
│       │   ├── middleware/
│       │   │   └── auth.ts       # 認証ミドルウェア
│       │   └── lib/
│       │       └── db.ts         # DB接続
│       ├── drizzle.config.ts
│       └── package.json
│
├── packages/
│   ├── db/                       # DBスキーマ・クライアント（共有）
│   │   ├── src/
│   │   │   ├── index.ts          # エクスポート
│   │   │   ├── schema.ts         # Drizzleスキーマ
│   │   │   └── client.ts         # Turso接続
│   │   ├── migrations/
│   │   ├── drizzle.config.ts
│   │   └── package.json
│   │
│   ├── ui/                       # 共有UIコンポーネント
│   │   ├── src/
│   │   │   ├── index.ts
│   │   │   └── components/
│   │   └── package.json
│   │
│   └── typescript-config/        # 共有TypeScript設定
│       ├── base.json
│       ├── nextjs.json
│       └── package.json
│
├── .claude/                      # Claude Code設定
│   └── settings.json
│
├── turbo.json                    # Turborepo設定
├── pnpm-workspace.yaml           # pnpmワークスペース
├── package.json                  # ルートpackage.json
├── .gitignore
├── .env.example
├── CLAUDE.md                     # Claude Code用ドキュメント
└── CONCEPT.md                    # このファイル
```

---

### 開発ステップ（予定）

| Step | 内容 | 学べること |
|------|------|------------|
| 1 | Turborepo + pnpm セットアップ | モノレポの基本構造 |
| 2 | packages/typescript-config 作成 | 共有設定の管理 |
| 3 | apps/web (Next.js 15) 作成 | App Router基礎 |
| 4 | Tailwind v4 + shadcn/ui 導入 | CSS-first設定 |
| 5 | packages/db + Drizzle + Turso | 型安全DB操作 |
| 6 | apps/api (Hono) 作成 | 軽量API構築 |
| 7 | Hono RPC でフロント連携 | 型安全API呼び出し |
| 8 | NextAuth (Auth.js) 導入 | 認証実装 |
| 9 | LiveRenderer実装 | コードプレビュー機能 |
| 10 | Claude Code設定 | .claude/, CLAUDE.md |

---

### 参考資料

- [Structuring a repository | Turborepo](https://turborepo.com/docs/crafting-your-repository/structuring-a-repository)
- [RPC - Hono](https://hono.dev/docs/guides/rpc)
- [Drizzle ORM - Drizzle with Turso](https://orm.drizzle.team/docs/tutorials/drizzle-with-turso)
- [Getting Started: Project Structure | Next.js](https://nextjs.org/docs/app/getting-started/project-structure)
- [Tailwind v4 - shadcn/ui](https://ui.shadcn.com/docs/tailwind-v4)
- [Auth.js | Drizzle Adapter](https://authjs.dev/getting-started/adapters/drizzle)
